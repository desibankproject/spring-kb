package com.anno;

public interface Vehicle {
	
	public void run();
	//Default method definition into the interface
	//Stream API example with filter
	//Lamda expression - Anonymous class
	//Enhancement in Date API
	//Enhancement in Generics where we can apply annotation parameter type
	//Enhancement in string classs
	//Enhancement in collection framework
	//support of functional interface
	//Change in JVM runtime data structure - metaspace
	//
	default public void walk(){
			System.out.println(")#@(#(@(@(@(@(@");
	}

}

class Ahaha implements Vehicle{

	@Override
	public void run() {
		
	}
	
}
